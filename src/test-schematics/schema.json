{
  "properties": {
    "name": {
      "type": "string",
      "x-prompt": "Define component name (without `component` suffix)",
      "default": "my-name"
    },
    "styleExtension": {
      "enum": ["css", "scss", "less"],
      "x-prompt": "Choose file name for styles",
      "default": "css"
    }
  }
}
